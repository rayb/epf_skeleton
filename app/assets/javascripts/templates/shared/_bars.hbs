<div class="row-fluid">
  <div class="span6">
    <legend>Bars</legend>

    <div class="row-fluid">
      <ul class="nav nav-pills nav-stacked">
        {{#each bar in bars}}
          {{view App.BarInListView contentBinding="bar"}}
        {{/each}}
      </ul>
    </div>

  </div>
  <div class="span6">
    {{ outlet }}
  </div>
</div>

